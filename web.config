<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <handlers>
      <!-- Node.js application handler for Hapi.js API -->
      <add name="iisnode" path="server/iis-startup.js" verb="*" modules="iisnode" />
    </handlers>
    
    <!-- iisnode configuration for Hapi.js with Node.js 20.21.0 -->
    <iisnode 
      node_env="production"
      nodeProcessCountPerApplication="2"
      maxConcurrentRequestsPerProcess="2048"
      maxNamedPipeConnectionRetry="5"
      namedPipeConnectionRetryDelay="1500"
      maxNamedPipeConnectionPoolSize="1024"
      maxNamedPipePooledConnectionAge="60000"
      asyncCompletionThreadCount="4"
      initialRequestBufferSize="8192"
      maxRequestBufferSize="131072"
      watchedFiles="*.js;*.json"
      uncFileChangesPollingInterval="3000"
      gracefulShutdownTimeout="30000"
      loggingEnabled="true"
      logDirectoryNameSuffix="logs"
      debuggingEnabled="false"
      devErrorsEnabled="false"
      enableXFF="true"
      promoteServerVars="HTTP_X_FORWARDED_FOR,HTTP_X_FORWARDED_PROTO"
    />

    <!-- Static file MIME types -->
    <staticContent>
      <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="30.00:00:00" />
    </staticContent>

    <!-- Default documents -->
    <defaultDocument>
      <files>
        <clear />
        <add value="public/index.html" />
        <add value="index.js" />
      </files>
    </defaultDocument>

    <!-- Directory browsing disabled -->
    <directoryBrowse enabled="false" />

    <rewrite>
      <rules>
        <!-- API routes - direct to Node.js -->
        <rule name="API Routes" stopProcessing="true">
          <match url="^api/(.*)" />
          <action type="Rewrite" url="server/iis-startup.js/{R:1}" />
        </rule>

        <!-- Static assets - serve directly with proper MIME types -->
        <rule name="Static Assets" stopProcessing="true">
          <match url="^assets/.*" />
          <action type="Rewrite" url="public/{R:0}" />
        </rule>

        <!-- Static files from dist/public - serve directly -->
        <rule name="Static Files" stopProcessing="true">
          <match url="^public/.*" />
          <action type="None" />
        </rule>

        <!-- Root request - serve React app -->
        <rule name="Root to React" stopProcessing="true">
          <match url="^$" />
          <action type="Rewrite" url="public/index.html" />
        </rule>
        
        <!-- All other routes (React SPA routing) -->
        <rule name="React SPA Routes" stopProcessing="true">
          <match url="^(?!api/).*" />
          <conditions>
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="public/index.html" />
        </rule>

      </rules>
    </rewrite>

    <!-- iisnode configuration -->
    <iisnode 
      nodeProcessCommandLine="%programfiles%\nodejs\node.exe"
      loggingEnabled="true"
      logDirectory="logs"
      debuggingEnabled="false"
      maxProcessCountPerApplication="1"
      maxConcurrentRequestsPerProcess="1024">
      
      <!-- Environment variables for SQL Server -->
      <environmentVariables>
        <add name="NODE_ENV" value="production" />
        <add name="DATABASE_URL" value="Server=localhost;Database=freelancing_platform;User Id=app_user;Password=Xman@123;Encrypt=true;TrustServerCertificate=true;" />
        <add name="DB_SERVER" value="localhost" />
        <add name="DB_PORT" value="1433" />
        <add name="DB_USER" value="app_user" />
        <add name="DB_PASSWORD" value="Xman@123" />
        <add name="DB_DATABASE" value="freelancing_platform" />
      </environmentVariables>
    </iisnode>

    <!-- Error pages -->
    <httpErrors>
      <remove statusCode="404" subStatusCode="-1" />
      <error statusCode="404" path="index.html" responseMode="ExecuteURL" />
    </httpErrors>

    <!-- Security headers -->
    <httpProtocol>
      <customHeaders>
        <add name="X-Content-Type-Options" value="nosniff" />
        <add name="X-Frame-Options" value="DENY" />
        <add name="X-XSS-Protection" value="1; mode=block" />
      </customHeaders>
    </httpProtocol>
  </system.webServer>
</configuration>