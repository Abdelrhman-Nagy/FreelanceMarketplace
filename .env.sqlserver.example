# SQL Server Database Configuration
# Copy this to .env.local and update with your SQL Server details

# Database Type - set to 'sqlserver' to use SQL Server instead of PostgreSQL
DB_TYPE=sqlserver

# SQL Server Connection Details
SQL_SERVER_HOST=your-server-host.database.windows.net
SQL_SERVER_USER=your-username
SQL_SERVER_PASSWORD=your-password
SQL_SERVER_DATABASE=FreelancingPlatform
SQL_SERVER_PORT=1433

# Session Configuration (required for authentication)
SESSION_SECRET=your-session-secret-here

# Replit Configuration (keep these as they are)
REPL_ID=your-repl-id
REPLIT_DOMAINS=your-domain.replit.app

# Optional: Keep PostgreSQL config as backup
# DATABASE_URL=your-postgresql-url